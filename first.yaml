name: shell commands
runs-on: ubuntu-latest
steps:
  - name: echo string
    run: echo "string"
  - name: multi line string
    run: |
      pwd
      ls -a
  - name: echo string
    run: echo "string"
  - name: multi line string
    run: |
      pwd
      node -v
      ls -a
